apiVersion: 1

providers:
  - name: 'POC Dashboards'
    orgId: 1
    folder: 'Service'
    folderUid: ''
    type: file
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards
